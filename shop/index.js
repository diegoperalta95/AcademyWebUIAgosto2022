/*
const sectionModal = document.getElementById("section-article-modal");
const ShowModal = function () {
    sectionModal.setAttribute("class", "show-modal")
}
const HideModal = function () {
    sectionModal.setAttribute("class", "hide-modal")
}
document.addEventListener("click",
    function (event) {
        if (event.target.matches("#backToShopBtn") || !event.target.closest(".contenedor")) {
            HideModal()
        }
    },
    false
)
document.addEventListener("click", 
function (e) {
    if(e.target.matches('.articleCard')){
        ShowModal();
    }
});

ShowModal();
*/